model Profile {
    id           String  @id @default(uuid())
    profilePhoto String?
    bio          String?

    useriId String @unique
    user    User   @relation(fields: [useriId], references: [id])

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("profiles")
}



















